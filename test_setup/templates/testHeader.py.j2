# This is an autogenerated test file. Modify at your own risk.
# Generated from manifest {{ manifestPath }}
import pytest
import asyncio
from asyncio import TimeoutError
from async_timeout import timeout
from typing import {% if chromeopts %}Dict,{% endif -%} List, Tuple
from test_setup.constants import (
    CHROME_EXE,
    OPERA_EXE,
    EDGE_EXE,
    FIREFOX_EXE,
    SAFARI_EXE,
    HAVE_PYWB,
    PYWB_SKIP_REASON,
    TRAVIS_TUNNEL_ID,
    USE_SAUCE,
    SAUCE_SKIP_REASON
)
from test_setup.loaders import load_file
from test_setup.wrtest import (
    BaseSimpleChromeTest,
    BaseWRSeleniumTest,
    WRAutoTestTimeOut,
)

{% if has_js %}
TEST_JS: str = (
    {{ test_util }}
    + "\n"
    + {{ js }}
)
{% else -%}
TEST_JS: str = {{ test_util }} + "\n"
{% endif -%}
{% if chromeopts %}
CHROME_OPTS: Dict[str, str]  = {{ chromeopts }}
{% endif -%}
TEST_LIST: List[str] = {{ test_list }}
PLAYER_INFO: Tuple[str, str, str] = (
    "{{ player_info[0] }}",
    "{{ player_info[1] }}",
    "{{ player_info[2] }}",
)
PLAYER_URL: str = "{{ player_url }}"
PYWB_URL: str = "{{ pywb_url }}"


