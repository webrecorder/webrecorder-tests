sudo: required

language: python
python:
  - "3.6"

branches:
  only:
  - master
  - develop

addons:
  chrome: stable

env:
  - CHROME="google-chrome-stable"

before_install:
  - ./bootstrap.sh
  - sudo apt-get -qq update
  - sudo apt install libgconf-2-4
  - wget -nc -O - https://chromedriver.storage.googleapis.com/2.31/chromedriver_linux64.zip | gunzip - > /usr/local/bin/chromedriver
  - chmod +x /usr/local/bin/chromedriver

install:
  - pip install -r requirements.txt

script: 
  - pytest -v -s
